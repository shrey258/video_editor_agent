NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
# Must match the backend's API_KEY. Server-side only (no NEXT_PUBLIC_ prefix) so it never reaches the browser.
API_KEY=
# Opens the dev/debug panel (token estimate tool) by default. Also toggleable in-app via Cmd/Ctrl+Shift+D.
NEXT_PUBLIC_DEV_PANEL=false
